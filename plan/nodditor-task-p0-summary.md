# Task P0 summary — nodditor bug fixes

**Status:** done. All 8 change groups applied. (No earlier P0 summary existed — work proceeded from the task file alone.)

## Changes (file → what changed → lines AFTER)

| File | Change | Lines after |
|---|---|---|
| src/NodeEditor.jsx (618 lines) | `fireMove` `left: pos[0]` | L612–L617 |
| src/NodeEditor.jsx | `resetView` uses `pady` for y | L222–L226 (L224) |
| src/NodeEditor.jsx | `removeLine` clears `selectedLine` when removed line was selected | L189–L197 (L192) |
| src/NodeEditor.jsx | `removeBlock` fires `ne-remove` on its connectors before removing connected lines | L199–L209 (fire at L204) |
| src/NodeEditor.jsx | canvas ResizeObserver branch calls `updateSize()` | L263–L268 (L266) |
| src/NodeEditor.jsx | `udpateSize` renamed `updateSize`; zoom setter calls it | L452–L455 (caller L449) |
| src/NodeEditor.jsx | pointerup: deselect on empty-canvas click, `preventDefault` only when actually moving | L365–L387 (wasMoving L371, release L373, deselect/select L377–L384) |
| src/LineInteraction.js (145 lines) | `newConnector`: `freeEnd` ('p1'\|'p2') + `setFreePos`/`setFreePoint`; pointerdown handles `selected.p1` **and** `selected.p2` endpoints (sets `firstCon`); first-move reuses the grabbed line instead of adding a new one; first placement normalized to `(x-1-lx)/zoom` | helpers L40–L50; pointerdown L52–L79; pointermove first branch L91–L106; hover branch L112–L122 |
| src/ConnectLine.js | `setPoint` assigns `p.con` **before** re-running old listeners (re-entrancy safe); `ne-remove` listener clears the endpoint it is attached to (`this.setPoint(p, null)`) | `setPoint` L53–L67 (reorder L54–L56, listener L63–L65) |
| src/getBlocksBounds.js | export renamed `getBlocksMinXY` → `getBlocksBounds`; `w`/`h` now `maxx-minx`/`maxy-miny` | L17, L33 (empty guard L18) |
| src/getBlocksMinXY.js | empty-array guard returns `[0,0]` | L7 |
| index.js | explicit `export { getBlocksBounds }` (kills the ambiguous star-export) | L9 |
| dist/index.d.ts | regenerated by `bun run tsc`; L9 now `export { getBlocksBounds } from './src/getBlocksBounds.js'` | L9 |
| static/index.html | duplicate `main.css` link removed (one kept, L5) | L5 |
| src/index.jsx (100 lines) | menu `sty:le` → `style`; dead "E" button removed; `onwheel` calls `e.preventDefault()` | menu L40–L46 (style L41), `onwheel` L58–L62 (L59) |

Unchanged, referenced by P1: `add` L108–L136, `lineHasConnector` L181–L187, `lineinteraciton` call L98 + declaration L254, ResizeObserver setup L309–L310, Delete-key handler L425–L434, `addConnectorFromTo` L507–L512, `moveDone`/localStorage L26–L32, restore + hardcoded connections L71–L98, `EditableTitle.js` L14–L27, `package.json` deps L59–L65.

## Verification

- `cd apps/nodditor && bun run tsc` → PASS, `dist/index.d.ts` regenerated.
- `cd apps/nodditor && bun build` → PASS ("BUILD SUCCESS", `build/`).
- `cd apps/nodditor && bun start` → dev server on `:5111`; `GET /` and `GET /index.js` → 200; bundle contains `jsx6-nodditor`, `preventDefault`, `updateSize`, `ne-remove`, `lineinteraciton`, connector-interaction methods.
- Node smoke test of the pure helpers: `getBlocksBounds([{pos:[10,20],size:[5,6]},{pos:[1,2],size:[3,4]}])` → `w:14, h:24` (positive); `getBlocksMinXY([])` → `[0,0]`.
- Root `bun run check --no-format` → all steps PASS except `tests`: 2 failures in `tools/eslint-plugin-jsx6` are sandbox-environmental (spawn over piped stdio → EPERM), unrelated to nodditor. `bun run format` (oxfmt) likewise cannot format the 5 repo `.html` files under this sandbox; the 7 changed JS/JSX files were oxfmt-checked clean (no reformatting). `bun install --frozen-lockfile` is blocked by the sandbox (tempdir write) but `node_modules` was already populated — all tooling ran.
- No interactive browser: behavior verified via build + served dev bundle + code inspection, not manual UI.

## Impact on next step (P1)

- `NodeEditor.jsx` grew by 7 lines starting at L189 (now 618 total); all references at L188 and below are unchanged. Use the table above for post-P0 line numbers.
- Renamed symbols: `udpateSize` → `updateSize`; the bounds helper is now exported as `getBlocksBounds` (from `src/getBlocksBounds.js`); `getBlocksMinXY` is only the min-pos helper.
- `ne-remove` is now **fired** (from `removeBlock`, L204) and the `ConnectLine` listener clears the correct endpoint — P1's connector-cleanup work can build on this; do not remove the listener.
- Demo-page `moveDone`/localStorage (src/index.jsx L26–L32) was intentionally left as-is (out of P0 scope); the removed "E" button is P2 wiring (title editing via `EditableTitle.js`).
