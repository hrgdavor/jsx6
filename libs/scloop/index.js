/**
 * @jsx6/scloop is a retired stub — it was published once as an empty module and is now marked
 * `"private": true`. See ./README.md; the feature it was reserved for lives in @jsx6/virtual-scroll.
 */
export {}