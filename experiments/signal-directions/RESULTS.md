# Direction comparison — measured results

Generated by `bun experiments/signal-directions/harness/compare.js` on 2026-09-24T10:00:00.638Z.

Every direction is exercised through the frozen facade in `contract.md`, and each one is
measured **in its own process** (`bench-one.js`) so JIT/GC state cannot leak between them.
Benchmarks are medians of 5 runs after a discarded warm-up, on the machine that generated this
file — treat the **relative** columns as the result, not the absolute milliseconds.

`own LOC` counts the direction directory's JavaScript, excluding tests; `+shared` is the
`_shared/` compat layer that A/E/F (and C's interop exports) reuse.

## Contract

| direction | pass | fail | skip | own LOC | +shared | dependencies |
|---|---|---|---|---|---|---|
| `baseline` | 22 | 0 | 24 | 339 | 116 | — |
| `a-compat` | 31 | 1 | 14 | 40 | 116 | alien-signals 3.2.1 (vendored, optional peer in a real release) |
| `b-native-computed` | 33 | 1 | 12 | 682 | 116 | — |
| `c-alien-backend` | 43 | 1 | 2 | 739 | 116 | alien-signals 3.2.1 (vendored; a real release would declare it in dependencies + catalog) |
| `e-alien-native` | 32 | 1 | 13 | 53 | 116 | alien-signals 3.2.1 (vendored, optional peer in a real release) |
| `f-bridge-hybrid` | 35 | 1 | 10 | 51 | 116 | alien-signals 3.2.1 (vendored, optional peer in a real release) |

## Capabilities

| capability | baseline | a-compat | b-native-computed | c-alien-backend | e-alien-native | f-bridge-hybrid |
|---|---|---|---|---|---|---|
| `core` | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| `union-deps` | · | ✓ | ✓ | ✓ | · | ✓ |
| `computed` | · | · | ✓ | ✓ | · | ✓ |
| `computed:auto` | · | · | ✓ | ✓ | · | · |
| `computed:dynamic-deps` | · | · | ✓ | ✓ | · | · |
| `computed:eager` | · | · | ✓ | ✓ | · | · |
| `computed:alien-only` | · | · | · | ✓ | ✓ | ✓ |
| `dispose` | · | · | ✓ | ✓ | · | · |
| `batch` | · | ✓ | ✓ | ✓ | ✓ | ✓ |
| `batch:core` | · | · | ✓ | ✓ | · | · |
| `interop` | · | ✓ | · | ✓ | ✓ | ✓ |
| `interop:bridge-required` | · | ✓ | · | · | ✓ | ✓ |
| `interop:no-bridge-needed` | · | · | · | ✓ | · | · |
| `alien-surface` | · | · | · | · | ✓ | · |

## Micro benchmarks (median ms)

| bench | baseline | a-compat | b-native-computed | c-alien-backend | e-alien-native | f-bridge-hybrid |
|---|---|---|---|---|---|---|
| write+read | 16.8 ms | 16.7 ms | 17.6 ms | 31.4 ms | 16.5 ms | 17.2 ms |
| fanout×100 | 31.0 ms | 31.3 ms | 34.6 ms | 31.8 ms | 33.2 ms | 32.2 ms |
| chain×4 | 5.35 ms | 5.73 ms | 5.89 ms | 16.6 ms | 5.58 ms | 5.78 ms |
| diamond | 7.42 ms | 8.21 ms | 8.93 ms | 18.1 ms | 8.42 ms | 8.46 ms |
| computed read | n/a | n/a | 2.00 ms | 4.48 ms | n/a | 7.63 ms |
| bind/unbind | 8.76 ms | 9.61 ms | 7.23 ms | 14.0 ms | 10.6 ms | 8.74 ms |
| $State merge | 4.74 ms | 8.31 ms | 7.85 ms | 15.8 ms | 8.81 ms | 8.06 ms |
| batch×2 writes | n/a | 3.43 ms | 3.81 ms | 6.93 ms | 3.48 ms | 3.39 ms |

### Relative to the untouched baseline

| bench | baseline | a-compat | b-native-computed | c-alien-backend | e-alien-native | f-bridge-hybrid |
|---|---|---|---|---|---|---|
| write+read | +0% | -1% | +4% | +87% | -2% | +2% |
| fanout×100 | +0% | +1% | +12% | +3% | +7% | +4% |
| chain×4 | +0% | +7% | +10% | +210% | +4% | +8% |
| diamond | +0% | +11% | +20% | +144% | +13% | +14% |
| computed read | n/a | n/a | n/a | n/a | n/a | n/a |
| bind/unbind | +0% | +10% | -17% | +60% | +21% | -0% |
| $State merge | +0% | +75% | +66% | +235% | +86% | +70% |
| batch×2 writes | n/a | n/a | n/a | n/a | n/a | n/a |

## Long-lived components

| metric | baseline | a-compat | b-native-computed | c-alien-backend | e-alien-native | f-bridge-hybrid |
|---|---|---|---|---|---|---|
| mount 300 (ms) | 6.12 ms | 8.42 ms | 8.13 ms | 8.70 ms | 8.68 ms | 8.78 ms |
| 3000 writes (ms) | 7.12 ms | 6.94 ms | 6.89 ms | 8.17 ms | 7.70 ms | 8.15 ms |
| unmount (ms) | 0.94 ms | 1.00 ms | 0.95 ms | 0.99 ms | 1.05 ms | 1.10 ms |
| heap while mounted (kB) | 2947 kB | 4265 kB | 3855 kB | 4276 kB | 4257 kB | 4255 kB |
| heap retained after unmount (kB) | 630 kB | 710 kB | 671 kB | 669 kB | 711 kB | 707 kB |

## Notes per direction

### `baseline` — baseline — frozen @jsx6/signal 1.8.18 (pre-graduation control)

* backend: pre-graduation eager core (snapshot)
* capabilities: core
* dependencies: —
* frozen snapshot of libs/signal as it was at 1.8.18, taken before B was graduated in place; the control every report compares against. Not edited afterwards.

### `a-compat` — A — compatibility / interop layer

* backend: untouched current core + alien bridge
* capabilities: core, union-deps, interop, interop:bridge-required, batch
* dependencies: alien-signals 3.2.1 (vendored, optional peer in a real release)
* does not change the core and therefore cannot auto-track: `$C` is absent, and a jsx6 signal read inside an alien computed must be bridged first (documented limitation). `batch` coalesces only the alien effect queue — it cannot defer the untouched core's own listener fan-out, which is why it does not claim `batch:core`.
* failing contract cases:
  * `core.state-snapshot`: expected {"x":1,"y":2}, got "{\"x\":1,\"y\":2}"

### `b-native-computed` — B — computed + auto-tracking + batch in the current core

* backend: copied current core (own graph, no dependency)
* capabilities: core, union-deps, computed, computed:auto, computed:dynamic-deps, computed:eager, dispose, batch, batch:core
* dependencies: —
* no dependency at all. `$C` is lazy, `$CE`/`$S`/`$F` are eager union-dependency computeds. Dependency edges reuse the existing listener Set; `batch` defers computed settle/notify in dependency order. No interop with alien-signals (that is A/E/F/C).
* failing contract cases:
  * `core.state-snapshot`: expected {"x":1,"y":2}, got "{\"x\":1,\"y\":2}"

### `c-alien-backend` — C — alien-signals as backend behind the jsx6 adapter

* backend: copied core rewritten on alien-signals 3.2.1
* capabilities: core, union-deps, computed, computed:auto, computed:dynamic-deps, computed:eager, dispose, batch, batch:core, interop, interop:no-bridge-needed, computed:alien-only
* dependencies: alien-signals 3.2.1 (vendored; a real release would declare it in dependencies + catalog)
* the only direction that ships a new runtime dependency. Eager `$S`/`$F` are kept (a root effect keeps each eager computed hot), so long-lived components behave as before. Opaque duck-typed deps (trans.js) fall back to the legacy derived path — the one place C is not fully alien-driven.
* failing contract cases:
  * `core.state-snapshot`: expected {"x":1,"y":2}, got "{\"x\":1,\"y\":2}"

### `e-alien-native` — E — alien-native surface ($C/$watch/$scope as-is)

* backend: untouched current core + alien API re-exported
* capabilities: core, interop, interop:bridge-required, batch, computed:alien-only, alien-surface
* dependencies: alien-signals 3.2.1 (vendored, optional peer in a real release)
* no core change at all. `$C`/`$watch`/`$scope`/`$trigger` are alien primitives, so they track alien values only — a jsx6 signal has to be bridged with toAlien() first. No dependency-list form, no eager mode, no repo-shaped dispose.
* failing contract cases:
  * `core.state-snapshot`: expected {"x":1,"y":2}, got "{\"x\":1,\"y\":2}"

### `f-bridge-hybrid` — F — bridge-only hybrid ($C over bridged deps)

* backend: untouched current core + alien computed over bridges
* capabilities: core, union-deps, interop, interop:bridge-required, batch, computed, computed:alien-only
* dependencies: alien-signals 3.2.1 (vendored, optional peer in a real release)
* ~15 lines on top of A. Lazy only (no eager mode), explicit dependencies required for jsx6 signals, no dispose (standalone alien computeds retain deps), ~26 % bridge overhead.
* failing contract cases:
  * `core.state-snapshot`: expected {"x":1,"y":2}, got "{\"x\":1,\"y\":2}"
