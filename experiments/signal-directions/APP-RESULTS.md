# Real-app comparison — `apps/repl` on each backend

Generated by `bun experiments/signal-directions/d-dual-surface/app-compare.js` on 2026-09-24T09:25:32.413Z.

The REPL is built **unmodified** with `@jsx6/signal` redirected to a core by an esbuild resolve
plugin, then booted headlessly and interrogated about what rendered. The `alias proof` column names
the implementation-specific string literal searched for in the bundle: `pre-1.9` must contain
neither marker, `shipped` (the workspace core) and `b-native-computed` must contain
`signalStateChildren`, and `signal-alien` / `c-alien-backend` must contain `signalAlienSource`.
A wrong result fails the row.

| backend | index.js | vs pre-1.9 | boot | app checks | alias proof | console.errors |
|---|---|---|---|---|---|---|
| `pre-1.9` | 49.3 kB | +0.0% | 16 ms | 10 / 10 | ok — `signalAlienSource` absent | 0 |
| `shipped` | 55.0 kB | +11.6% | 33 ms | 10 / 10 | ok — `signalStateChildren` present | 0 |
| `signal-alien` | 70.6 kB | +43.2% | 27 ms | 10 / 10 | ok — `signalAlienSource` present | 0 |

App checks asserted: bundle boots without throwing · static children rendered · a `Jsx6` class
component and a function component rendered · `Loop` items set through the scope (`p` attribute) ·
scope captured `editor`/`editor3` parts · `x-value` inputs present · `x-if` reacts to a signal
write **synchronously** · a `Loop` re-renders after an async signal write · a **Promise used
directly as a JSX child** resolves into the DOM.

Entries built: index.jsx, editor.jsx, demistify.jsx, random.tricks.jsx. Full bundle totals are in each run's console output.