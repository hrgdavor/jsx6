# Real-app comparison — `apps/repl` on each backend

Generated by `bun experiments/signal-directions/d-dual-surface/app-compare.js` on 2026-09-23T21:31:43.372Z.

The REPL is built **unmodified** with `@jsx6/signal` redirected to a candidate core by an
esbuild resolve plugin, then booted headlessly and interrogated about what rendered. The
`alias proof` column names the implementation-specific string literal searched for in the bundle:
the baseline must **not** contain either marker, B must contain `signalStateChildren`, C must
contain `signalAlienSource`. A wrong result fails the row.

| backend | index.js | vs baseline | boot | app checks | alias proof | console.errors |
|---|---|---|---|---|---|---|
| `baseline` | 49.2 kB | +0.0% | 13 ms | 10 / 10 | ok — `signalAlienSource` absent | 0 |
| `b-native-computed` | 54.4 kB | +10.6% | 29 ms | 10 / 10 | ok — `signalStateChildren` present | 0 |
| `c-alien-backend` | 64.3 kB | +30.8% | 26 ms | 10 / 10 | ok — `signalAlienSource` present | 0 |

App checks asserted: bundle boots without throwing · static children rendered · a `Jsx6` class
component and a function component rendered · `Loop` items set through the scope (`p` attribute) ·
scope captured `editor`/`editor3` parts · `x-value` inputs present · `x-if` reacts to a signal
write **synchronously** · a `Loop` re-renders after an async signal write · a **Promise used
directly as a JSX child** resolves into the DOM.

Entries built: index.jsx, editor.jsx, demistify.jsx, random.tricks.jsx. Full bundle totals are in each run's console output.