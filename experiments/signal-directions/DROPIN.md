# Drop-in verification: can the candidate replace `@jsx6/signal` as-is?

Generated by `bun experiments/signal-directions/harness/dropin.js` on 2026-09-24T10:00:03.897Z.

"Drop-in" is checked, not asserted: export-surface parity, behavioural parity on every contract case
the shipped package passes, deep-source-path compatibility, and the untouched `libs/signal` test
suite. A single behavioural regression fails the verdict.

## Verdict

| candidate | surface | behaviour | source paths | types (`tsc`) | bundles | shipped tests | verdict |
|---|---|---|---|---|---|---|---|
| `libs/signal-alien` | 55 names, all matching | 43 cases pass, 0 regressions | all present | clean, no declarations lost | esm + cjs built | green (exit 0) | ✅ **drop-in** |
| `c-alien-backend` | 55 names, all matching | 43 cases pass, 0 regressions | all present | clean, no declarations lost | esm + cjs built | green (exit 0) | ✅ **drop-in** |
| `b-native-computed` | 55 names, all matching | 33 cases pass, 0 regressions | all present | clean, no declarations lost | esm + cjs built | green (exit 0) | ✅ **drop-in** |

## 1. Export surface

The shipped package exports 55 names. Each candidate must provide all of
them with the same `typeof`.

### `libs/signal-alien`

* shipped names present: **55 / 55**
* missing: — none
* type mismatches: — none
* **added** by this direction (7): `alien`, `isAlienComputed`, `isAlienNode`, `isAlienSignal`, `stateSymbol`, `toAlien`, `toSignal`

### `c-alien-backend`

* shipped names present: **55 / 55**
* missing: — none
* type mismatches: — none
* **added** by this direction (8): `alien`, `isAlienComputed`, `isAlienNode`, `isAlienSignal`, `meta`, `stateSymbol`, `toAlien`, `toSignal`

### `b-native-computed`

* shipped names present: **55 / 55**
* missing: — none
* type mismatches: — none
* **added** by this direction (1): `meta`

## 2. Behavioural parity (contract cases)

Cases the shipped package passes are the compatibility bar: a candidate that fails one is a
regression. Cases the shipped package cannot pass are the new capability.

### `libs/signal-alien`

* regressions vs the shipped package: **none**
* newly passing (added capability, 10): `computed.alien-auto-track`, `batch.alien-path`, `interop.toAlien`, `interop.unbridged-tracks-natively`, `interop.toSignal`, `interop.observe-alien-directly`, `interop.effect-reads-jsx6`, `interop.footgun-safe`, `interop.alien-dep-in-derived`, `interop.state-child-inside-alien`
* still not applicable/skipped (2): `alien-surface.effect-and-scope`, `interop.unbridged-does-not-track`

### `c-alien-backend`

* regressions vs the shipped package: **none**
* newly passing (added capability, 10): `computed.alien-auto-track`, `batch.alien-path`, `interop.toAlien`, `interop.unbridged-tracks-natively`, `interop.toSignal`, `interop.observe-alien-directly`, `interop.effect-reads-jsx6`, `interop.footgun-safe`, `interop.alien-dep-in-derived`, `interop.state-child-inside-alien`
* still not applicable/skipped (2): `alien-surface.effect-and-scope`, `interop.unbridged-does-not-track`

### `b-native-computed`

* regressions vs the shipped package: **none**
* newly passing (added capability, 0): —
* still not applicable/skipped (12): `computed.alien-auto-track`, `batch.alien-path`, `alien-surface.effect-and-scope`, `interop.toAlien`, `interop.unbridged-does-not-track`, `interop.unbridged-tracks-natively`, `interop.toSignal`, `interop.observe-alien-directly`, `interop.effect-reads-jsx6`, `interop.footgun-safe`, `interop.alien-dep-in-derived`, `interop.state-child-inside-alien`

## 3. Packaging contract

The manifest ships `index.js`, `src`, `dist`, `esm`, `cjs` and lists `src` as importable. A drop-in
replacement must keep every shipped path resolvable with the same relative layout, so deep imports
and the publish gate (`scripts/check-manifests.js`) keep working.

| path shipped in `libs/signal` | in `libs/signal-alien` | in `c-alien-backend` | in `b-native-computed` |
|---|---|---|---|
| `src/signal.js` (source) | **missing** | ✓ | ✓ |
| `src/observe.js` (source) | **missing** | ✓ | ✓ |
| `src/state.js` (source) | **missing** | ✓ | ✓ |
| `src/makeContext.js` (source) | **missing** | ✓ | ✓ |
| `index.js` (source) | **missing** | ✓ | ✓ |
| `dist/index.d.ts` (generated) | **not built** | ✓ built | ✓ built |
| `esm/index.js` (generated) | **not built** | ✓ built | ✓ built |
| `cjs/index.js` (generated) | **not built** | ✓ built | ✓ built |

### Publishability: types and bundles

The manifest and `tsconfig.json` are build configuration, so they are **copied from `libs/signal`**
into each candidate (nothing in `libs/` is touched), and then the real publish steps are run:

```bash
cd experiments/signal-directions/<candidate>/signal
bun x tsc -p tsconfig.json     # declaration emit + checkJs type checking
bun run build && bun run build-cjs
```

#### `libs/signal-alien`

* `tsc` (declarations **and** `checkJs` type checking): clean
* `build` (esm) / `build-cjs`: ok / ok
* declarations the shipped package has and the candidate's emitted `dist/index.d.ts` lacks: **none** (31 baseline declarations)
* declarations **added** by the candidate: `alien`, `toAlien`, `toSignal`, `isAlienSignal`, `isAlienComputed`, `isAlienNode`

#### `c-alien-backend`

* `tsc` (declarations **and** `checkJs` type checking): clean
* `build` (esm) / `build-cjs`: ok / ok
* declarations the shipped package has and the candidate's emitted `dist/index.d.ts` lacks: **none** (31 baseline declarations)
* declarations **added** by the candidate: `alien`, `toAlien`, `toSignal`, `isAlienSignal`, `isAlienComputed`, `isAlienNode`

#### `b-native-computed`

* `tsc` (declarations **and** `checkJs` type checking): clean
* `build` (esm) / `build-cjs`: ok / ok
* declarations the shipped package has and the candidate's emitted `dist/index.d.ts` lacks: **none** (31 baseline declarations)
* declarations **added** by the candidate: — none

Type-surface parity matters because `dist/index.d.ts` is published: a changed declaration is a
public API change for TypeScript consumers even when the runtime behaviour is identical.

Two packaging facts are **not** parity-checkable here and must be handled at graduation:

1. **A new runtime dependency.** C imports alien-signals, so `libs/signal/package.json` gains a
   `dependencies` entry and the root `catalog` gains a pin; the published package stops having zero
   dependencies. B adds nothing. This is the only respect in which C is not a drop-in *package* even
   though it is a drop-in *API*.
2. **CDN / raw-entry resolution.** The raw `index.js` is the `import`/`module` entry. With C it
   contains a bare `alien-signals` specifier, which a browser cannot resolve without an import map;
   the bundled `esm`/`cjs` entries inline it and are unaffected. Mitigation is a README note plus
   making the bundled entry the documented CDN one.

## 4. Consumer proof

* `libs/signal`'s own 7 test files / 30 tests, copied byte-identically into each candidate, run in
  the candidate's core:
  * `libs/signal-alien`: **green**
  * `c-alien-backend`: **green**
  * `b-native-computed`: **green**
* the real `apps/repl` entry (built unmodified with `@jsx6/signal` aliased to the candidate) boots and
  passes every app check — see `APP-RESULTS.md`.

## 5. What a drop-in verdict does *not* claim

* It does not claim identical *performance*: C costs +90 % on a raw write+read microbenchmark (each
  read consults the alien node when a subscriber is active) while being within ~14 % on the
  long-lived-component macrobench. See `RESULTS.md`.
* It does not claim identical *internals*: `prepareSignal` still returns
  `{ $signal, fireChanged, listeners, setValue }` with the same semantics, but in C the storage is an
  alien node. Code that writes to the underlying node behind the wrapper's back would desynchronise
  the `===` shadow — nothing in the repo does that.
* It does not claim the new computed axis is available in the baseline sense: in C, `$C` is an alien
  computed, so it follows alien's rules (pull-based, read-only, no eager mode except `$CE`).